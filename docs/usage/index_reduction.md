# Index Reduction

`torchdae`'s Index Reduction supports `differentiation` and `dummy_derivative` modes.

The `differentiation` takes the derivative of the equation depending on the `differentiation_orders` of the inputted equations
